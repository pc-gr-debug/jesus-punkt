# Fix Round Implementation Plan (2026-07-06)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the approved fix round in `docs/superpowers/specs/2026-07-06-fix-round-design.md`: data layer, homepage layout fixes, 150px section rhythm, Werte scroll wheel, header rework with sliding hover pill, per-page heroes, events calendar, team cards, Gemeindeleben declutter + galleries, Spenden/Kontakt cleanup, docs.

**Architecture:** Static vanilla site. One new module `js/data.js` renders every `data-ct` slot from `/data/mock/*.json`; `js/main.js` keeps behavior (header, nav, carousels → generalized, wheel, glider, galleries). CSS stays in the token → base → components → home|pages cascade.

**Tech Stack:** HTML/CSS/vanilla JS, no build step. Serve via `python3 -m http.server 8000` from the project root (root-absolute paths). Screenshots via `node tools/screenshot.js '[{"url":…,"w":…,"h":…,"out":…}]'`.

## Global Constraints

- NOT a git repository — no commit steps; each task ends with a verification step instead.
- All sizing `rem` (px ÷ 16); `px` only for 1px hairlines. No hard-coded px widths/heights.
- Only tokens from `styles/tokens.css`; no new colors.
- Every text node wrapped in a `<div>` cover element; style covers, not text nodes.
- Sections: inner `.container` div, never pad the section itself.
- Breakpoints: `>64rem` desktop · `≤64rem` · `≤48rem` · `≤37.5rem` · `≤30rem`; scale progressively.
- Hover animations ≤450ms with `--ease`; respect `prefers-reduced-motion` (base.css already kills all transitions globally under it — new JS must ALSO gate behavior).
- No `overflow: hidden` on body/html (exception: existing `html.menu-open`).
- Keep every `data-ct` attribute in the DOM.
- German UI copy, `·` as separator character.
- Test URL base: `http://localhost:8000`.

**Server (start once before verification steps):**
```bash
cd "/Users/mykhailo/Desktop/jesus punkt" && python3 -m http.server 8000 &
```

---

### Task 1: Mock data — extend events.json, add team.json

**Files:**
- Modify: `data/mock/events.json` (replace whole file)
- Create: `data/mock/team.json`

**Interfaces:**
- Produces: `events.json` → `{ events: [{ id, start (ISO 8601 local), type ("gottesdienst"|"jugend"|"gebet"|"hauskreis"|"sonstiges"), title, meta, url? }] }` — display date/time strings are NO LONGER stored, they are derived in JS.
- Produces: `team.json` → `{ people: [{ id, name, roles: string[], photo: string|null, featured?: bool, note?: string }] }`

- [x] **Step 1: Replace `data/mock/events.json`** — ISO dates spanning Jul–Sep 2026 (≥3 months for the calendar; Sundays 10:00 Gottesdienst weekly, plus the existing variety):

```json
{
  "_comment": "Reference shape for GET /api/events (ChurchTools proxy) — see docs/churchtools-integration.md. start = ISO 8601 local time; display strings are derived client-side (Intl de-DE).",
  "events": [
    { "id": 1001, "start": "2026-07-12T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1002, "start": "2026-07-17T18:30:00", "type": "jugend", "title": "Elevate Jugend", "meta": "Für Jugendliche zwischen 13 und 29 Jahren" },
    { "id": 1003, "start": "2026-07-19T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1004, "start": "2026-07-22T19:30:00", "type": "hauskreis", "title": "Hauskreis-Abend", "meta": "In Wohnzimmern in ganz Neubrandenburg" },
    { "id": 1005, "start": "2026-07-24T18:30:00", "type": "jugend", "title": "Elevate Jugend", "meta": "Für Jugendliche zwischen 13 und 29 Jahren" },
    { "id": 1006, "start": "2026-07-25T13:00:00", "type": "gebet", "title": "Gebet", "meta": "Gemeinsames Gebet · alle sind willkommen" },
    { "id": 1007, "start": "2026-07-26T10:00:00", "type": "gottesdienst", "title": "Gottesdienst mit Taufe", "meta": "Ein besonderer Sonntag — bring gern jemanden mit" },
    { "id": 1008, "start": "2026-08-02T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1009, "start": "2026-08-07T18:30:00", "type": "jugend", "title": "Elevate Jugend", "meta": "Für Jugendliche zwischen 13 und 29 Jahren" },
    { "id": 1010, "start": "2026-08-09T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1011, "start": "2026-08-15T14:00:00", "type": "sonstiges", "title": "Gemeindegrillen", "meta": "Für die ganze Familie · am Gemeindehaus" },
    { "id": 1012, "start": "2026-08-16T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1013, "start": "2026-08-22T13:00:00", "type": "gebet", "title": "Gebet", "meta": "Gemeinsames Gebet · alle sind willkommen" },
    { "id": 1014, "start": "2026-08-23T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1015, "start": "2026-08-30T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1016, "start": "2026-09-04T18:30:00", "type": "jugend", "title": "Elevate Jugend", "meta": "Start nach den Ferien" },
    { "id": 1017, "start": "2026-09-06T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1018, "start": "2026-09-13T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1019, "start": "2026-09-19T13:00:00", "type": "gebet", "title": "Gebet", "meta": "Gemeinsames Gebet · alle sind willkommen" },
    { "id": 1020, "start": "2026-09-20T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" },
    { "id": 1021, "start": "2026-09-27T10:00:00", "type": "gottesdienst", "title": "Gottesdienst", "meta": "Kruseshofer Str. 20 · vor Ort und im Livestream" }
  ]
}
```

- [x] **Step 2: Create `data/mock/team.json`** — one entry per unique person, duplicates merged, pairs split:

```json
{
  "_comment": "Reference shape for GET /api/team (ChurchTools persons + group roles) — one entry per person, all roles aggregated.",
  "people": [
    { "id": "philipp-strauch", "name": "Philipp Strauch", "roles": ["Pastor", "Gottesdienst", "Jugendbereich"], "photo": null, "featured": true,
      "note": "Leitet die Gemeinde seit Oktober 2025. Philipp brennt dafür, das Wort Gottes zu predigen und zu sehen, wie Menschen in ihrer Berufung und ihrem Glauben wachsen." },
    { "id": "tymur-sheikh", "name": "Tymur Sheikh", "roles": ["Lobpreis"], "photo": null },
    { "id": "arne-brockmann", "name": "Arne Brockmann", "roles": ["Website"], "photo": null },
    { "id": "lydia-wegner", "name": "Lydia Wegner", "roles": ["Kindergottesdienst"], "photo": null },
    { "id": "anja-maas", "name": "Anja Maas", "roles": ["Kindergottesdienst"], "photo": null },
    { "id": "claus-wittnebel", "name": "Claus Wittnebel", "roles": ["Jugendbereich"], "photo": null },
    { "id": "ulrike-huhn", "name": "Ulrike Huhn", "roles": ["Hauswirtschaft"], "photo": null },
    { "id": "guenther-seidt", "name": "Günther Seidt", "roles": ["Finanzen"], "photo": null },
    { "id": "saskia-lobert", "name": "Saskia Lobert", "roles": ["Technik"], "photo": null },
    { "id": "yurii-uglov", "name": "Yurii Uglov", "roles": ["Hausmeister"], "photo": null }
  ]
}
```

- [x] **Step 3: Verify** both parse:
```bash
python3 -c "import json;[json.load(open(f)) for f in ['data/mock/events.json','data/mock/team.json']];print('OK')"
```
Expected: `OK`

---

### Task 2: `js/data.js` — fetch + render layer

**Files:**
- Create: `js/data.js`
- Modify: all 8 HTML files — add `<script src="/js/data.js" defer></script>` after the `main.js` script tag (index, ueber-uns, events, gemeindeleben, angebote, spenden, kontakt, impressum; angebote/spenden/kontakt/impressum have no dynamic slots but load it harmlessly for future slots — SKIP adding it there; add ONLY to index.html, ueber-uns/index.html, events/index.html, gemeindeleben/index.html).

**Interfaces:**
- Consumes: Task 1 JSON schemas.
- Produces: globals `window.JP = { formatEvent(e), renderEvents, renderCalendar, renderSermons(list, trackEl), initialized via DOMContentLoaded }` — internal only; later tasks rely on these **behaviors**, not exports:
  - `[data-ct="events"]` on index → exactly 3 `<li>` event rows (see Task 3 for layout contract).
  - `[data-ct="events"][data-calendar]` on events page → month agenda (Task 8 provides the surrounding controls with ids `cal-prev`, `cal-next`, `cal-label`).
  - `[data-ct="sermons"]` (carousel track `<ul>`) → sermon `<li class="carousel__item">` items.
  - `[data-ct="team"]` → lead card into `[data-ct="team-lead"]`, grid `<li>`s into `[data-ct="team"]`.
  - `[data-ct="flyer"] img` src/alt refreshed from `flyer.json`.
  - `[data-ct="groups"]` → kg-row `<li>`s.
- Produces: event row markup identical to the existing `.event-row` component (see index.html lines 93–129 for reference shape).

- [x] **Step 1: Create `js/data.js`** with this exact content:

```js
/* Jesus Punkt — data layer · renders data-ct slots from mock JSON.
   Live ChurchTools swap: change the URL map below only. */
(function () {
  'use strict';

  var URLS = {
    events: '/data/mock/events.json',
    flyer: '/data/mock/flyer.json',
    sermons: '/data/mock/sermons.json',
    groups: '/data/mock/groups.json',
    team: '/data/mock/team.json'
  };

  /* "now" for mock data — keeps the demo deterministic while the JSON is static.
     Set to null to use the real clock once live data is wired up. */
  var NOW = new Date('2026-07-06T12:00:00');
  function now() { return NOW || new Date(); }

  var WD = new Intl.DateTimeFormat('de-DE', { weekday: 'short' });   // "So."
  var DM = new Intl.DateTimeFormat('de-DE', { day: '2-digit', month: 'short' }); // "12. Juli" → short: "12. Jul."
  var MY = new Intl.DateTimeFormat('de-DE', { month: 'long', year: 'numeric' }); // "Juli 2026"
  var TIME = new Intl.DateTimeFormat('de-DE', { hour: '2-digit', minute: '2-digit' }); // "10:00"

  function fmtWeekdayDate(d) {
    var wd = WD.format(d).replace('.', '');
    var dm = DM.format(d).replace(/\.$/, '');
    return wd + ' ' + dm; // "So 12. Jul"
  }
  function fmtTime(d) { return TIME.format(d); }

  function el(html) {
    var t = document.createElement('template');
    t.innerHTML = html.trim();
    return t.content.firstElementChild;
  }
  function esc(s) {
    return String(s).replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c];
    });
  }

  function eventRowHTML(ev, highlight, href) {
    var d = new Date(ev.start);
    var inner =
      '<div class="event-row__when">' +
        '<div><span class="event-row__time">' + fmtTime(d) + '</span></div>' +
        '<div><span class="event-row__date">' + fmtWeekdayDate(d) + '</span></div>' +
      '</div>' +
      '<div class="event-row__body">' +
        '<div><h3 class="event-row__title">' + esc(ev.title) + '</h3></div>' +
        '<div><p class="event-row__meta">' + esc(ev.meta) + '</p></div>' +
      '</div>';
    var cls = 'event-row' + (highlight ? ' event-row--highlight' : '');
    return href
      ? '<li><a class="' + cls + '" href="' + href + '">' + inner + '</a></li>'
      : '<li><div class="' + cls + '">' + inner + '</div></li>';
  }

  function getJSON(url) {
    return fetch(url).then(function (r) {
      if (!r.ok) throw new Error(url + ' → ' + r.status);
      return r.json();
    });
  }

  /* ---------- homepage: flyer + exactly 3 next events ---------- */
  function renderHomeEvents(data) {
    var slot = document.querySelector('[data-ct="events"]:not([data-calendar])');
    if (!slot) return;
    var upcoming = data.events
      .filter(function (e) { return new Date(e.start) >= now(); })
      .sort(function (a, b) { return new Date(a.start) - new Date(b.start); });
    var service = upcoming.filter(function (e) { return e.type === 'gottesdienst'; })[0];
    var others = upcoming.filter(function (e) { return e !== service && e.type !== 'gottesdienst'; }).slice(0, 2);
    if (!service && !others.length) return;
    var rows = [];
    if (service) rows.push(eventRowHTML(service, true, '/events/'));
    others.forEach(function (e) { rows.push(eventRowHTML(e, false, '/events/')); });
    slot.innerHTML = rows.join('');
  }

  /* ---------- events page: month slider + agenda ---------- */
  function initCalendar(data) {
    var slot = document.querySelector('[data-ct="events"][data-calendar]');
    var label = document.getElementById('cal-label');
    var prev = document.getElementById('cal-prev');
    var next = document.getElementById('cal-next');
    if (!slot || !label || !prev || !next) return;

    var events = data.events.slice().sort(function (a, b) {
      return new Date(a.start) - new Date(b.start);
    });
    if (!events.length) return;
    function monthKey(d) { return d.getFullYear() * 12 + d.getMonth(); }
    var first = new Date(events[0].start);
    var last = new Date(events[events.length - 1].start);
    var minM = monthKey(first), maxM = monthKey(last);
    var cur = Math.min(Math.max(monthKey(now()), minM), maxM);

    function render() {
      var y = Math.floor(cur / 12), m = cur % 12;
      label.textContent = MY.format(new Date(y, m, 1));
      prev.disabled = cur <= minM;
      next.disabled = cur >= maxM;
      var monthEvents = events.filter(function (e) {
        var d = new Date(e.start);
        return d.getFullYear() === y && d.getMonth() === m;
      });
      if (!monthEvents.length) {
        slot.innerHTML = '<li><div class="calendar__empty"><p>In diesem Monat stehen keine Termine im Kalender — schau gern im nächsten Monat vorbei.</p></div></li>';
        return;
      }
      slot.innerHTML = monthEvents.map(function (e) {
        return eventRowHTML(e, e.type === 'gottesdienst', null);
      }).join('');
    }
    prev.addEventListener('click', function () { if (cur > minM) { cur--; render(); } });
    next.addEventListener('click', function () { if (cur < maxM) { cur++; render(); } });
    render();
  }

  /* ---------- sermons (carousel tracks on any page) ---------- */
  function renderSermons(data) {
    document.querySelectorAll('[data-ct="sermons"]').forEach(function (track) {
      track.innerHTML = data.sermons.map(function (s) {
        return (
          '<li class="carousel__item">' +
            '<a class="card" href="' + esc(s.url) + '" target="_blank" rel="noopener">' +
              '<div class="card__media" aria-hidden="true">' +
                (s.thumb ? '<img src="' + esc(s.thumb) + '" alt="" loading="lazy">' : '') +
              '</div>' +
              '<div class="card__body">' +
                '<div><span class="card__date">' + esc(s.date) + '</span></div>' +
                '<div class="card__group">' +
                  '<div><h3 class="card__title">' + esc(s.title) + '</h3></div>' +
                  '<div><p class="card__meta">' + esc(s.speaker) + '</p></div>' +
                '</div>' +
              '</div>' +
            '</a>' +
          '</li>'
        );
      }).join('');
      if (window.JPCarousel) window.JPCarousel.refresh(track);
    });
  }

  /* ---------- flyer ---------- */
  function renderFlyer(data) {
    document.querySelectorAll('[data-ct="flyer"] img').forEach(function (img) {
      img.src = '/' + String(data.url).replace(/^\//, '');
      img.alt = data.alt;
    });
  }

  /* ---------- groups ---------- */
  function renderGroups(data) {
    document.querySelectorAll('[data-ct="groups"]').forEach(function (list) {
      var href = list.closest('#hauskreise') ? '#hauskreise' : '/gemeindeleben/#hauskreise';
      list.innerHTML = data.groups.map(function (g) {
        return (
          '<li><a class="kg-row" href="' + href + '">' +
            '<div class="kg-row__info">' +
              '<div><h3 class="kg-row__name">' + esc(g.name) + '</h3></div>' +
              '<div><p class="kg-row__meta">' + esc(g.meeting) + '</p></div>' +
            '</div>' +
            '<div class="kg-row__arrow" aria-hidden="true">' +
              '<svg width="62" height="12" viewBox="0 0 62 12" fill="none"><path d="M0 6h59.5M55 1.5 60.5 6 55 10.5" stroke="currentColor" stroke-width="2"/></svg>' +
            '</div>' +
          '</a></li>'
        );
      }).join('');
    });
  }

  /* ---------- team ---------- */
  function initials(name) {
    return name.split(/\s+/).map(function (p) { return p[0]; }).join('').slice(0, 2).toUpperCase();
  }
  function renderTeam(data) {
    var lead = document.querySelector('[data-ct="team-lead"]');
    var grid = document.querySelector('[data-ct="team"]');
    if (!grid) return;
    var featured = data.people.filter(function (p) { return p.featured; })[0];
    var rest = data.people.filter(function (p) { return p !== featured; });
    if (lead && featured) {
      lead.innerHTML =
        '<div class="lead-card">' +
          '<div class="team-photo team-photo--lead">' +
            (featured.photo ? '<img src="' + esc(featured.photo) + '" alt="' + esc(featured.name) + '" loading="lazy">'
                            : '<span class="team-photo__initials">' + initials(featured.name) + '</span>') +
          '</div>' +
          '<div class="lead-card__body">' +
            '<div><span class="lead-card__name">' + esc(featured.name) + '</span></div>' +
            '<div class="role-chips">' + featured.roles.map(function (r) {
              return '<span class="role-chip">' + esc(r) + '</span>';
            }).join('') + '</div>' +
            (featured.note ? '<div class="lead-card__note"><p>' + esc(featured.note) + '</p></div>' : '') +
          '</div>' +
        '</div>';
    }
    grid.innerHTML = rest.map(function (p) {
      return (
        '<li><div class="team-card">' +
          '<div class="team-photo">' +
            (p.photo ? '<img src="' + esc(p.photo) + '" alt="' + esc(p.name) + '" loading="lazy">'
                     : '<span class="team-photo__initials">' + initials(p.name) + '</span>') +
          '</div>' +
          '<div class="team-card__body">' +
            '<div><span class="team-card__name">' + esc(p.name) + '</span></div>' +
            '<div class="role-chips">' + p.roles.map(function (r) {
              return '<span class="role-chip">' + esc(r) + '</span>';
            }).join('') + '</div>' +
          '</div>' +
        '</div></li>'
      );
    }).join('');
  }

  /* ---------- boot: fetch only what the page needs ---------- */
  function boot() {
    if (document.querySelector('[data-ct="events"]')) {
      getJSON(URLS.events).then(function (d) {
        renderHomeEvents(d);
        initCalendar(d);
      }).catch(function (e) { console.warn('[data] events:', e.message); });
    }
    if (document.querySelector('[data-ct="flyer"]')) {
      getJSON(URLS.flyer).then(renderFlyer).catch(function (e) { console.warn('[data] flyer:', e.message); });
    }
    if (document.querySelector('[data-ct="sermons"]')) {
      getJSON(URLS.sermons).then(renderSermons).catch(function (e) { console.warn('[data] sermons:', e.message); });
    }
    if (document.querySelector('[data-ct="groups"]')) {
      getJSON(URLS.groups).then(renderGroups).catch(function (e) { console.warn('[data] groups:', e.message); });
    }
    if (document.querySelector('[data-ct="team"]')) {
      getJSON(URLS.team).then(renderTeam).catch(function (e) { console.warn('[data] team:', e.message); });
    }
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot);
  else boot();
})();
```

Note: `el()` helper is unused if all renders go through innerHTML — delete it before saving. `renderSermons` calls `window.JPCarousel.refresh` which Task 8 defines; guard (`if (window.JPCarousel)`) keeps it safe before Task 8 lands.

- [x] **Step 2: Syntax check** — `node --check js/data.js` → no output (exit 0).

- [x] **Step 3: Add the script tag** to `index.html`, `ueber-uns/index.html`, `events/index.html`, `gemeindeleben/index.html`. In each, replace:
```html
<script src="/js/main.js" defer></script>
```
with:
```html
<script src="/js/main.js" defer></script>
<script src="/js/data.js" defer></script>
```

- [x] **Step 4: Verify in browser** — with server running, load `http://localhost:8000/` and check console shows no `[data]` warnings and the events list still shows 3 rows (Gottesdienst first, highlighted).

---

### Task 3: Homepage layout — equal columns, section rhythm, eyebrow dots, footer

**Files:**
- Modify: `styles/tokens.css:59` (`--section-pad`)
- Modify: `styles/home.css` (events grid, eyebrow separators)
- Modify: `styles/components.css` (footer padding, footer non-link affordance, nav-panel nums — CSS side)
- Modify: `index.html` (eyebrow markup)

**Interfaces:**
- Consumes: Task 2 renders 3 rows into the homepage events list.
- Produces: `--section-pad` new value used by all sections; `.hero__eyebrow-part + .hero__eyebrow-part::before` dot separator pattern.

- [x] **Step 1: tokens.css** — replace:
```css
  --section-pad: clamp(4.5rem, 6.94vw, 6.25rem); /* 100 desktop → 72 phone */
```
with:
```css
  --section-pad: clamp(6rem, 10.42vw, 9.375rem); /* 150 desktop → 96 phone */
```

- [x] **Step 2: home.css events grid** — replace:
```css
.events__grid {
  margin-top: var(--space-xl);
  display: grid;
  grid-template-columns: minmax(0, 32.8125rem) minmax(0, 1fr); /* 525 | 535 */
  gap: var(--space-2xl);
  align-items: start;
}
```
with:
```css
.events__grid {
  margin-top: var(--space-xl);
  display: grid;
  grid-template-columns: 1fr 1fr; /* flyer matches the events column */
  gap: var(--space-2xl);
  align-items: start;
}
.events__flyer { aspect-ratio: auto; align-self: stretch; }
.events__flyer img { object-position: center; }
```
(`.events__flyer` stretches to the events-column height so both columns read as one block; base `.flyer-card` keeps its 525/297 ratio elsewhere.)

- [x] **Step 3: index.html eyebrow** — replace:
```html
          <span><span class="hero__eyebrow-part">Sonntags 10:00 Uhr</span>&ensp;<span class="hero__eyebrow-part">Kruseshofer Str. 20</span>&ensp;<span class="hero__eyebrow-part">17036 Neubrandenburg</span></span>
```
with:
```html
          <span><span class="hero__eyebrow-part">Sonntags 10:00 Uhr</span> <span class="hero__eyebrow-part">Kruseshofer Str. 20</span> <span class="hero__eyebrow-part">17036 Neubrandenburg</span></span>
```
and in `styles/home.css` after `.hero__eyebrow-part { white-space: nowrap; }` add:
```css
.hero__eyebrow-part + .hero__eyebrow-part::before {
  content: "·";
  color: var(--gold-text);
  margin-inline: 0.55em 0.65em;
}
```

- [x] **Step 4: footer** — in `styles/components.css` replace:
```css
.footer {
  background: var(--night);
  color: var(--creme-82);
  padding: 5rem 0 2.5rem;
}
```
with:
```css
.footer {
  background: var(--night);
  color: var(--creme-82);
  padding: 6.5rem 0 3rem;
}
```
and replace:
```css
.footer__grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2.125rem;
}
```
with:
```css
.footer__grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2.75rem;
}
```
and replace:
```css
.footer__legal {
  margin-top: var(--space-xl);
```
with:
```css
.footer__legal {
  margin-top: 4rem;
```
Non-link affordance: plain `span.footer__item` already has no hover styles (only `a.footer__item` does) — verify no `cursor: pointer` anywhere on spans; nothing to change unless found.

- [x] **Step 5: Verify** — reload `http://localhost:8000/`: equal 50/50 event/flyer columns, `·` between eyebrow parts, taller sections and footer. Screenshot check:
```bash
node tools/screenshot.js '[{"url":"http://localhost:8000/","w":1440,"h":2400,"out":"docs/superpowers/plans/shots/t3-home.png"}]'
```

---

### Task 4: Hamburger digits + Next-Steps deletion

**Files:**
- Modify: all 8 HTML files (nav-panel digits)
- Modify: `index.html` (delete Next-Steps section), `gemeindeleben/index.html` (delete Next-Steps section)
- Modify: `styles/home.css` (delete `.next-steps*` + `.step*` blocks), `styles/pages.css` (delete `.steps-grid` + `.step*` block)
- Modify: `design-system.html` (remove step/next-steps demo if present)

**Interfaces:** none downstream.

- [x] **Step 1: digits** — in every one of the 8 HTML files, each nav-panel link loses its num span. Pattern (repeat for all 6 links × 8 files — use the bulk-multipage-edit skill):
OLD: `<a class="nav-panel__link" href="/ueber-uns/"><span class="nav-panel__num">01</span>Über uns</a>`
NEW: `<a class="nav-panel__link" href="/ueber-uns/">Über uns</a>`
(also the `is-active` variants on each subpage). Then delete from `styles/components.css`:
```css
.nav-panel__num { font: 700 0.75rem / 1 var(--font-ui); color: var(--gold-text); }
```

- [x] **Step 2: delete Next-Steps sections** — in `index.html` remove the whole block from `<!-- ================= NEXT STEPS ================= -->` through its closing `</section>` (lines 386–423). Same in `gemeindeleben/index.html` (lines 218–261, includes the `flyer-nextsteps` aside).

- [x] **Step 3: delete dead CSS** — `styles/home.css`: remove the entire `/* ===== NEXT STEPS ===== */` block (`.next-steps` … `.next-steps__foot .btn { width: 100%; }` including both media queries) and the `.next-steps` mention in the final phone CTA rule if present. `styles/pages.css`: remove `/* ---- steps (vier Fragen…) ---- */` block (`.steps-grid`, `.step`, `.step__num`, `.step__title`, `.step__text`) and the `.steps-grid` lines inside the two media queries.

- [x] **Step 4: Verify** —
```bash
grep -rn "next-steps\|steps-grid\|step__\|nav-panel__num" index.html gemeindeleben/index.html styles/ design-system.html | grep -v "Binary"
```
Expected: no matches (design-system checked/cleaned in Task 12).

---

### Task 5: Header rework — no strip, chips, sliding glider, dot active

**Files:**
- Modify: `styles/components.css` (header block, nav-pill block)
- Modify: `js/main.js` (replace header scroll handler comment scope; add glider module)

**Interfaces:**
- Consumes: existing `.header`, `.nav-pill`, `.burger` markup (unchanged in HTML).
- Produces: `.nav-pill` gains `position: relative`; JS injects `<span class="nav-pill__glider">` as first child of `.nav-pill`. `.is-scrolled` remains the scroll-state class.

- [x] **Step 1: components.css header** — replace lines 6–17 (`.header { … }` and `.header.is-scrolled { … }`) with:

```css
.header {
  position: fixed;
  inset: 0 0 auto 0;
  z-index: 30;
}
/* scrolled: the floating elements carry their own surfaces — no full-width strip */
.header.is-scrolled .nav-pill {
  background: rgba(255, 255, 255, 0.88);
  box-shadow: inset 0 0 0 1px var(--line-ink), var(--shadow-soft);
}
.header.is-scrolled .header__logo,
.header.is-scrolled .burger {
  background: rgba(255, 255, 255, 0.88);
  -webkit-backdrop-filter: blur(1.25rem);
  backdrop-filter: blur(1.25rem);
  box-shadow: inset 0 0 0 1px var(--line-ink), var(--shadow-soft);
}
```

- [x] **Step 2: logo chip base** — replace:
```css
.header__logo { display: block; flex: none; transition: transform var(--t-med) var(--ease); }
```
with:
```css
.header__logo {
  display: block;
  flex: none;
  padding: 0.375rem 0.75rem;
  border-radius: var(--radius);
  transition: transform var(--t-med) var(--ease), background var(--t-med) var(--ease),
              box-shadow var(--t-med) var(--ease);
}
```
(burger already has its own background + radius; the scrolled rule only strengthens it.)

- [x] **Step 3: nav pill + glider + active dot** — replace the whole `.nav-pill` / `.nav-pill__link` / hover block (lines 29–50) with:

```css
.nav-pill {
  position: relative;
  display: flex;
  align-items: center;
  background: rgba(255, 255, 255, 0.75);
  -webkit-backdrop-filter: blur(1.25rem);
  backdrop-filter: blur(1.25rem);
  border-radius: var(--radius);
  transition: background var(--t-med) var(--ease), box-shadow var(--t-med) var(--ease);
}
/* sliding hover highlight — positioned by JS */
.nav-pill__glider {
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  width: 0;
  border-radius: var(--radius);
  background: var(--accent);
  opacity: 0;
  pointer-events: none;
  transition: left 300ms var(--ease), width 300ms var(--ease), opacity 200ms var(--ease);
}
.nav-pill.has-glider .nav-pill__glider { opacity: 1; }
.nav-pill__link {
  position: relative;
  z-index: 1;
  display: block;
  padding: 0.75rem 1.5rem;
  border-radius: var(--radius);
  font: 400 var(--fs-nav) / 1 var(--font-body);
  color: var(--nav-muted);
  white-space: nowrap;
  transition: color var(--t-fast) var(--ease);
}
.nav-pill__link:hover { color: #000; }
/* current page: ink text + gold dot — no background */
.nav-pill__link.is-active { color: var(--ink); }
.nav-pill__link.is-active::after {
  content: "";
  display: inline-block;
  width: 0.3125rem;
  height: 0.3125rem;
  border-radius: 50%;
  background: var(--accent);
  margin-left: 0.375rem;
  vertical-align: 0.06em;
}
/* no-JS / reduced-motion fallback keeps a visible hover */
@media (prefers-reduced-motion: reduce) {
  .nav-pill__link:hover { background: var(--accent); color: #000; }
}
```

- [x] **Step 4: main.js glider** — in `js/main.js`, after the header scroll block (after line 13 `}`), insert:

```js
  /* ---------- Nav pill: sliding hover highlight ---------- */
  var pill = document.querySelector('.nav-pill');
  var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
  if (pill && !reduceMotion.matches) {
    var glider = document.createElement('span');
    glider.className = 'nav-pill__glider';
    glider.setAttribute('aria-hidden', 'true');
    pill.prepend(glider);

    var moveTo = function (link) {
      glider.style.left = link.offsetLeft + 'px';
      glider.style.width = link.offsetWidth + 'px';
    };
    var show = function (link) {
      if (!pill.classList.contains('has-glider')) {
        /* first entry: appear in place, no slide-in from 0 */
        glider.style.transition = 'none';
        moveTo(link);
        void glider.offsetWidth; /* flush so the next transition animates */
        glider.style.transition = '';
        pill.classList.add('has-glider');
      } else {
        moveTo(link);
      }
    };
    var hide = function () { pill.classList.remove('has-glider'); };

    pill.querySelectorAll('.nav-pill__link').forEach(function (link) {
      link.addEventListener('mouseenter', function () { show(link); });
      link.addEventListener('focus', function () { show(link); });
      link.addEventListener('blur', hide);
    });
    pill.addEventListener('mouseleave', hide);
  }
```

- [x] **Step 5: Verify** — `node --check js/main.js` → exit 0. In the browser at 1440w: transparent header over hero; after scrolling, pill/logo/burger get their surfaces but NO full-width strip; hovering nav links slides one gold pill between them; on `/events/` the Events label is ink with a gold dot and no background. Keyboard: Tab through nav links moves the glider.

---

### Task 6: Sieben Werte wheel

**Files:**
- Modify: `index.html` (replace the whole `#werte` section, lines 317–384)
- Modify: `styles/home.css` (replace the whole WERTE block incl. media queries, lines 147–266)
- Modify: `js/main.js` (append wheel module)

**Interfaces:**
- Consumes: bubble color variant classes (`bubble--sand` etc.) — kept.
- Produces: section structure `.werte > .werte__sticky > .container` with `.wheel` (7 `.wheel__item`) and `.werte__panel`; JS drives `--wheel-turn` custom property + `is-active` item class + panel content swap.

- [x] **Step 1: index.html** — replace the entire `<!-- ===== WERTE (floating bubbles) ===== -->` section with:

```html
  <!-- ================= WERTE (scroll wheel) ================= -->
  <section class="section werte" id="werte" aria-labelledby="werte-h">
    <div class="werte__sticky">
      <div class="container">
        <div class="section-head werte__head">
          <div class="section-head__title">
            <h2 class="h2" id="werte-h">Sieben Werte, die uns tragen</h2>
          </div>
          <div class="section-head__desc">
            <p>Was uns als Gemeinde prägt — an jedem Sonntag und an allen Tagen dazwischen.</p>
          </div>
        </div>
        <div class="werte__stage">
          <ul class="wheel" role="list">
            <li class="wheel__item" style="--i: 0"><div class="bubble bubble--sand"><div><span class="bubble__num">01</span></div><div><h3 class="bubble__name">Authentisch</h3></div></div></li>
            <li class="wheel__item" style="--i: 1"><div class="bubble bubble--accent"><div><span class="bubble__num">02</span></div><div><h3 class="bubble__name">Relevant</h3></div></div></li>
            <li class="wheel__item" style="--i: 2"><div class="bubble bubble--paper"><div><span class="bubble__num">03</span></div><div><h3 class="bubble__name">Hingegeben</h3></div></div></li>
            <li class="wheel__item" style="--i: 3"><div class="bubble bubble--ink"><div><span class="bubble__num">04</span></div><div><h3 class="bubble__name">Gastfreundlich</h3></div></div></li>
            <li class="wheel__item" style="--i: 4"><div class="bubble bubble--gold"><div><span class="bubble__num">05</span></div><div><h3 class="bubble__name">Begeistert</h3></div></div></li>
            <li class="wheel__item" style="--i: 5"><div class="bubble bubble--surface"><div><span class="bubble__num">06</span></div><div><h3 class="bubble__name">Großzügig</h3></div></div></li>
            <li class="wheel__item" style="--i: 6"><div class="bubble bubble--outline"><div><span class="bubble__num">07</span></div><div><h3 class="bubble__name">Wertschätzend</h3></div></div></li>
          </ul>
          <div class="werte__panel" aria-live="polite">
            <div><span class="werte__panel-num">01</span></div>
            <div><h3 class="werte__panel-name">Authentisch</h3></div>
            <div class="werte__panel-line"><p>Wir leben, was wir sagen.</p></div>
          </div>
        </div>
      </div>
    </div>
  </section>
```

The 7 value lines live in JS (single source, panel swaps them):
`["Wir leben, was wir sagen.", "Wir sind am Puls der Zeit.", "Wir geben Gott unser Bestes, nachdem er sein Bestes für uns gegeben hat.", "Wir schaffen eine liebevolle „Welcome Home“-Atmosphäre.", "Die Freude an Gott ist unsere Stärke.", "Wir segnen, weil wir gesegnet sind.", "Wir lieben Menschen."]`

- [x] **Step 2: home.css** — replace the entire WERTE block (from `/* ===== WERTE (floating bubbles) ===== */` through the `@media (max-width: 37.5rem)` werte rules, lines 147–266) with:

```css
/* ================= WERTE (scroll wheel) ================= */
.werte {
  padding-block: 0;           /* the sticky stage provides its own vertical rhythm */
  height: 350vh;              /* scroll runway: 7 stops */
}
.werte__sticky {
  position: sticky;
  top: 0;
  height: 100dvh;
  display: flex;
  align-items: center;
  overflow: hidden;           /* wheel edges may poke out of the stage, not the page */
}
.werte__sticky > .container { width: 100%; }
.werte__head { align-items: center; text-align: center; }
.werte__stage {
  margin-top: var(--space-xl);
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 24rem);
  gap: var(--space-2xl);
  align-items: center;
}

/* wheel geometry: items sit on a ring, ring turns via --wheel-turn */
.wheel {
  --wheel-size: min(26rem, 38vw);
  --wheel-turn: 0deg;
  position: relative;
  width: var(--wheel-size);
  height: var(--wheel-size);
  margin-inline: auto;
  transform: rotate(var(--wheel-turn));
  transition: transform var(--t-slow) var(--ease);
}
.wheel__item {
  --angle: calc(var(--i) * 51.4286deg);
  position: absolute;
  top: 50%;
  left: 50%;
  width: 9.5rem;
  height: 9.5rem;
  margin: -4.75rem 0 0 -4.75rem;
  transform: rotate(var(--angle))
             translateY(calc(var(--wheel-size) / -2))
             rotate(calc(-1 * var(--angle) - var(--wheel-turn)));
  transition: transform var(--t-slow) var(--ease), opacity var(--t-slow) var(--ease);
  opacity: 0.55;
}
.wheel__item .bubble { padding: 12%; gap: 0.25rem; }
.wheel__item.is-active {
  opacity: 1;
  transform: rotate(var(--angle))
             translateY(calc(var(--wheel-size) / -2))
             rotate(calc(-1 * var(--angle) - var(--wheel-turn)))
             scale(1.18);
  z-index: 1;
}
.wheel__item.is-active .bubble { box-shadow: 0 0 0 3px var(--accent), var(--shadow-soft); }

/* description panel */
.werte__panel { display: flex; flex-direction: column; gap: 0.625rem; }
.werte__panel-num { font: 700 0.8125rem / 1 var(--font-ui); color: var(--gold-text); }
.werte__panel-name { font: 700 var(--fs-h2) / var(--lh-heading) var(--font-display); color: var(--ink); }
.werte__panel-line { max-width: 24rem; color: var(--ink-2); }
.werte__panel.is-switching { }
.werte__panel > div {
  transition: opacity 350ms var(--ease), transform 350ms var(--ease);
}
.werte__panel.is-switching > div { opacity: 0; transform: translateY(0.375rem); }

@media (max-width: 64rem) {
  .werte__stage { gap: var(--space-xl); grid-template-columns: minmax(0, 1fr) minmax(0, 19rem); }
}
@media (max-width: 48rem) {
  .werte { height: 300vh; }
  .werte__stage {
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    justify-items: center;
    text-align: center;
  }
  .wheel { --wheel-size: min(17rem, 62vw); }
  .wheel__item { width: 6.5rem; height: 6.5rem; margin: -3.25rem 0 0 -3.25rem; }
  .wheel__item .bubble__name { font-size: 0.8125rem; }
  .werte__panel { align-items: center; min-height: 9rem; }
}

/* reduced motion: plain stacked list, no pin, no rotation */
@media (prefers-reduced-motion: reduce) {
  .werte { height: auto; padding-block: var(--section-pad); }
  .werte__sticky { position: static; height: auto; overflow: visible; display: block; }
  .werte__stage { display: flex; flex-direction: column; align-items: center; gap: var(--space-lg); }
  .wheel { width: auto; height: auto; transform: none; display: flex; flex-direction: column; gap: var(--space-md); margin: 0; }
  .wheel__item { position: static; margin: 0; transform: none !important; opacity: 1; width: auto; height: auto; }
  .wheel__item .bubble { width: 12rem; height: 12rem; }
  .werte__panel { display: none; }
}
```
Delete the now-unused rules elsewhere in home.css: `.werte__field`, `.werte__bubble*`, `.werte__dot*`, `@keyframes bubble-drift`, and the `.bubble__line` rules (the `.bubble` base + color variants STAY — the wheel and reduced-motion list use them). Keep `.bubble__line` styles only if still referenced; the new markup drops `bubble__line` inside bubbles, so remove `.bubble__line` and its hover rule too (the ink/gold variant lines referencing `.bubble__line` inside color variants also go).

- [x] **Step 3: main.js wheel module** — append before the closing `})();`:

```js
  /* ---------- Werte wheel (pinned scroll section) ---------- */
  var werte = document.querySelector('.werte');
  if (werte && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    var wheel = werte.querySelector('.wheel');
    var items = wheel ? Array.prototype.slice.call(wheel.querySelectorAll('.wheel__item')) : [];
    var panel = werte.querySelector('.werte__panel');
    var LINES = [
      'Wir leben, was wir sagen.',
      'Wir sind am Puls der Zeit.',
      'Wir geben Gott unser Bestes, nachdem er sein Bestes für uns gegeben hat.',
      'Wir schaffen eine liebevolle „Welcome Home“-Atmosphäre.',
      'Die Freude an Gott ist unsere Stärke.',
      'Wir segnen, weil wir gesegnet sind.',
      'Wir lieben Menschen.'
    ];
    if (wheel && items.length === 7 && panel) {
      var numEl = panel.querySelector('.werte__panel-num');
      var nameEl = panel.querySelector('.werte__panel-name');
      var lineEl = panel.querySelector('.werte__panel-line p');
      var active = -1;
      var switchTimer;

      var setActive = function (idx) {
        if (idx === active) return;
        active = idx;
        wheel.style.setProperty('--wheel-turn', (-idx * 51.4286) + 'deg');
        items.forEach(function (it, i) { it.classList.toggle('is-active', i === idx); });
        panel.classList.add('is-switching');
        clearTimeout(switchTimer);
        switchTimer = setTimeout(function () {
          numEl.textContent = '0' + (idx + 1);
          nameEl.textContent = items[idx].querySelector('.bubble__name').textContent;
          lineEl.textContent = LINES[idx];
          panel.classList.remove('is-switching');
        }, 180);
      };

      var onWerteScroll = function () {
        var rect = werte.getBoundingClientRect();
        var runway = rect.height - window.innerHeight;
        if (runway <= 0) { setActive(0); return; }
        var progress = Math.min(1, Math.max(0, -rect.top / runway));
        setActive(Math.min(6, Math.floor(progress * 7)));
      };
      window.addEventListener('scroll', onWerteScroll, { passive: true });
      onWerteScroll();
    }
  }
```

- [x] **Step 4: Verify** — `node --check js/main.js` → exit 0. Browser at 1440w: scrolling through the Werte section pins the stage; each ~1/7 of the runway rotates the wheel one step; top bubble scales up with gold ring; panel crossfades number/name/line; after the 7th value the page unpins. At 390w the wheel is centered with panel below. With macOS “Reduce Motion” on: flat list, no pin.


---

### Task 7: Per-page heroes

**Files:**
- Create: `img/hero-ueber-uns.jpg`, `img/hero-events.jpg`, `img/hero-gemeindeleben.jpg` (from assets/current-site)
- Modify: `styles/components.css` (replace PAGE HEAD block with PAGE HERO)
- Modify: `ueber-uns/index.html`, `events/index.html`, `gemeindeleben/index.html`, `angebote/index.html`, `spenden/index.html`, `kontakt/index.html`, `impressum/index.html` (page-head → page-hero)

**Interfaces:**
- Produces: `.page-hero` component (title class `.page-hero__title`, desc `.page-hero__desc`). `.page-head` is deleted — nothing else may reference it afterwards.

- [x] **Step 1: images** (placeholders until real photos delivered; sips re-encode strips metadata):
```bash
sips -Z 1800 -s format jpeg -s formatOptions 62 assets/current-site/community.jpg --out img/hero-ueber-uns.jpg
sips -Z 1800 -s format jpeg -s formatOptions 62 assets/current-site/worship-guitar.jpg --out img/hero-events.jpg
sips -Z 1800 -s format jpeg -s formatOptions 62 assets/current-site/recent-1.jpg --out img/hero-gemeindeleben.jpg
file img/hero-*.jpg   # all: JPEG image data
```

- [x] **Step 2: components.css** — replace the whole `/* ===== PAGE HEAD (subpages) ===== */` block (lines 138–151) with:

```css
/* ================= PAGE HERO (subpages) ================= */
.page-hero {
  position: relative;
  min-height: 50dvh;
  display: flex;
  align-items: flex-end;
  overflow: hidden;
  background: var(--paper);
}
.page-hero__bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.48;
}
.page-hero__wash {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.5) 0%, #ffffff 100%);
}
.page-hero__inner {
  position: relative;
  width: 100%;
  padding-top: calc(var(--header-h) + 3rem);
  padding-bottom: clamp(2.5rem, 6vh, 4rem);
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: var(--space-md);
}
.page-hero__title {
  font: 700 clamp(2.375rem, 1.5rem + 3.1vw, 3.75rem) / var(--lh-heading) var(--font-display);
  letter-spacing: -0.025em;
  color: var(--ink);
}
.page-hero__desc { max-width: 43.75rem; color: var(--ink-2); }

@media (max-width: 48rem) {
  .page-hero { min-height: 42dvh; }
}
```

- [x] **Step 3: swap markup on all 7 subpages.** Template — replace:
```html
  <section class="page-head">
    <div class="container page-head__inner">
      <div><h1 class="page-head__title h2--dot">TITLE</h1></div>
      <div class="page-head__desc"><p>DESC</p></div>
    </div>
  </section>
```
with:
```html
  <section class="page-hero">
    <img class="page-hero__bg" src="IMG" alt="" fetchpriority="high">
    <div class="page-hero__wash" aria-hidden="true"></div>
    <div class="container page-hero__inner">
      <div><h1 class="page-hero__title h2--dot">TITLE</h1></div>
      <div class="page-hero__desc"><p>DESC</p></div>
    </div>
  </section>
```
TITLE/DESC stay per page. IMG per page: ueber-uns → `/img/hero-ueber-uns.jpg` · events → `/img/hero-events.jpg` · gemeindeleben → `/img/hero-gemeindeleben.jpg` · angebote, spenden, kontakt, impressum → `/img/hero-dome.jpg`. Use the bulk-multipage-edit skill (variant per page).

- [x] **Step 4: Verify** —
```bash
grep -rln "page-head" ueber-uns events gemeindeleben angebote spenden kontakt impressum styles design-system.html
```
Expected: at most `design-system.html` (cleaned in Task 12). Browser: each subpage opens with an image hero, title readable, header transparent over it.

---

### Task 8: Events page — calendar + full-bleed sermon carousel; carousel generalization

**Files:**
- Modify: `js/main.js` (generalize carousel; expose `window.JPCarousel.refresh`)
- Modify: `index.html` (carousel nav buttons: ids → data attributes)
- Modify: `events/index.html` (Veranstaltungen → Kalender layout; Predigten → carousel; drop tint)
- Modify: `styles/components.css` (add `.events__list` shared rule)
- Modify: `styles/home.css` (remove `.events__list` rule)
- Modify: `styles/pages.css` (calendar bar styles; `.section-head + .feature-grid` rhythm rule)

**Interfaces:**
- Consumes: Task 2's `initCalendar` (needs `#cal-prev`, `#cal-next`, `#cal-label`, `[data-ct="events"][data-calendar]`) and `renderSermons` (calls `window.JPCarousel.refresh(track)`).
- Produces: `window.JPCarousel = { refresh(trackEl) }`; carousels are any `.carousel[id]` with buttons `[data-carousel-prev="<id>"]` / `[data-carousel-next="<id>"]`.

- [x] **Step 1: main.js** — replace the whole sermon-carousel block (lines 41–83, from `/* ---------- Sermon carousel (full-bleed) ---------- */` to the end of `render();`) with:

```js
  /* ---------- Carousels (full-bleed) — one init per .carousel[id] ---------- */
  function setupCarousel(carousel) {
    var track = carousel.querySelector('.carousel__track');
    var prevBtn = document.querySelector('[data-carousel-prev="' + carousel.id + '"]');
    var nextBtn = document.querySelector('[data-carousel-next="' + carousel.id + '"]');
    if (!track || !prevBtn || !nextBtn) return;
    var index = 0;

    function metrics() {
      var items = track.children;
      if (!items.length) return null;
      var itemW = items[0].getBoundingClientRect().width;
      var gap = parseFloat(getComputedStyle(track).columnGap || getComputedStyle(track).gap) || 0;
      var usable = track.getBoundingClientRect().width;
      var perView = Math.max(1, Math.round((usable + gap) / (itemW + gap)));
      return { step: itemW + gap, max: Math.max(0, items.length - perView) };
    }

    function render() {
      var m = metrics();
      if (!m) return;
      index = Math.min(index, m.max);
      track.style.transform = 'translateX(' + (-index * m.step) + 'px)';
      prevBtn.disabled = index <= 0;
      nextBtn.disabled = index >= m.max;
    }

    prevBtn.addEventListener('click', function () { index = Math.max(0, index - 1); render(); });
    nextBtn.addEventListener('click', function () {
      var m = metrics();
      if (m) { index = Math.min(m.max, index + 1); render(); }
    });

    var resizeTimer;
    window.addEventListener('resize', function () {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(render, 120);
    });

    carousel._render = render;
    render();
  }
  document.querySelectorAll('.carousel[id]').forEach(setupCarousel);
  window.JPCarousel = {
    refresh: function (track) {
      var c = track.closest('.carousel');
      if (c && c._render) c._render();
    }
  };
```

- [x] **Step 2: index.html buttons** — replace:
```html
          <button class="icon-btn" id="sermons-prev" aria-label="Vorherige Predigten" disabled>
```
with:
```html
          <button class="icon-btn" data-carousel-prev="sermons-carousel" aria-label="Vorherige Predigten" disabled>
```
and:
```html
          <button class="icon-btn" id="sermons-next" aria-label="Weitere Predigten">
```
with:
```html
          <button class="icon-btn" data-carousel-next="sermons-carousel" aria-label="Weitere Predigten">
```

- [x] **Step 3: shared `.events__list`** — add to `styles/components.css` directly above the `/* ===== EVENT ROW ===== */` rule:
```css
.events__list { display: flex; flex-direction: column; gap: 1.125rem; }
```
and delete the identical `.events__list { … }` rule from `styles/home.css`.

- [x] **Step 4: pages.css** — append:
```css
/* ---- calendar (Events page) ---- */
.section-head + .feature-grid { margin-top: var(--space-xl); }
.calendar { display: flex; flex-direction: column; gap: var(--space-lg); }
.calendar__bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-md);
}
.calendar__label { font: 700 var(--fs-h3) / 1.2 var(--font-display); color: var(--ink); min-width: 11rem; text-align: center; }
.calendar__empty {
  padding: 2.5rem 1.5rem;
  border: 1px dashed var(--line);
  border-radius: var(--radius);
  color: var(--ink-2);
  text-align: center;
}
```

- [x] **Step 5: events/index.html Veranstaltungen section** — replace the `feature-grid` block (lines 70–154, `<div class="feature-grid" style="margin-top: var(--space-xl)">` … closing `</div>` of feature-aside) with:

```html
      <div class="feature-grid">
        <div class="calendar">
          <div class="calendar__bar">
            <button class="icon-btn" id="cal-prev" aria-label="Vorheriger Monat">
              <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M9 2 L2 10 L9 18" stroke="currentColor" stroke-width="1.5"/></svg>
            </button>
            <div><h3 class="calendar__label" id="cal-label">Juli 2026</h3></div>
            <button class="icon-btn" id="cal-next" aria-label="Nächster Monat">
              <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M2 2 L9 10 L2 18" stroke="currentColor" stroke-width="1.5"/></svg>
            </button>
          </div>
          <ul class="events__list" data-ct="events" data-calendar></ul>
        </div>
        <div class="feature-aside">
          <div><span class="eyebrow">Aktueller Flyer</span></div>
          <div class="flyer-card flyer-card--tall" data-ct="flyer">
            <img src="/img/flyer-aktuell.jpg" alt="Aktueller Flyer zum Sonntagsgottesdienst" loading="lazy">
          </div>
          <div class="section-head__desc"><p>Der Flyer zum Sonntagsgottesdienst — jede Woche frisch aus ChurchTools.</p></div>
        </div>
      </div>
```
Also update the section description (line 68): replace `Die kommenden Termine im Überblick.` with `Alle Termine im Monatskalender.` (rest of the sentence unchanged). Add to `styles/pages.css` under the calendar block:
```css
.flyer-card--tall { aspect-ratio: 3 / 4; }
```

- [x] **Step 6: events/index.html Predigten section** — replace the whole section (from `<section class="section section--tint" id="predigten"` through its `</section>`) with the homepage carousel pattern (namespaced id):

```html
  <section class="section" id="predigten" aria-labelledby="predigten-h">
    <div class="container">
      <div class="sermons__head">
        <div class="section-head">
          <div class="section-head__title"><h2 class="h2" id="predigten-h">Predigten</h2></div>
          <div class="section-head__desc"><p>Die letzten Predigten zum Nachhören und Weiterdenken — wann und wo du willst.</p></div>
        </div>
        <div class="sermons__nav">
          <button class="icon-btn" data-carousel-prev="predigten-carousel" aria-label="Vorherige Predigten" disabled>
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M9 2 L2 10 L9 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
          <button class="icon-btn" data-carousel-next="predigten-carousel" aria-label="Weitere Predigten">
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M2 2 L9 10 L2 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="carousel sermons__carousel" id="predigten-carousel">
        <ul class="carousel__track" data-ct="sermons"></ul>
      </div>
      <div class="sermons__foot">
        <a class="btn btn--outline" href="https://jesus-punkt.de/media/" target="_blank" rel="noopener">Komplettes Predigtarchiv</a>
      </div>
    </div>
  </section>
```
`sermons__head/nav/carousel/foot` classes come from home.css which the events page does NOT load — move the `/* ===== PREDIGTEN ===== */` block (`.sermons__head`, `.sermons__nav`, `.sermons__carousel`, `.sermons__foot` + its two media queries) from `styles/home.css` into `styles/components.css` (below the carousel block), and delete it from home.css. Remove the `.cards-grid` block from pages.css (no longer used) and its mentions in the pages.css media queries.

- [x] **Step 7: Verify** — `node --check js/main.js` → exit 0. Browser `http://localhost:8000/events/`: hero, month label »Juli 2026«, ‹ › steps through Jul–Sep (‹ disabled in Jul, › disabled in Sep), agenda rows with Gottesdienst highlighted, empty-state absent in months with events; Predigten renders as full-bleed carousel on white; homepage carousel still works. Console clean.


---

### Task 9: Über uns — team cards

**Files:**
- Modify: `ueber-uns/index.html` (team section)
- Modify: `styles/pages.css` (add team-card/team-photo/role-chip; delete `.person` block)

**Interfaces:**
- Consumes: Task 2 `renderTeam` → fills `[data-ct="team-lead"]` and `[data-ct="team"]` with `.lead-card`/`.team-card` markup (classes defined here).
- Produces: `.team-card`, `.team-photo`, `.team-photo--lead`, `.team-photo__initials`, `.role-chips`, `.role-chip`.

- [x] **Step 1: pages.css** — after the `.lead-card__note` rule add:

```css
/* photo-first team cards (rendered from team.json) */
.team-photo {
  aspect-ratio: 3 / 4;
  background: var(--sand);
  display: grid;
  place-items: center;
  overflow: hidden;
}
.team-photo img { width: 100%; height: 100%; object-fit: cover; }
.team-photo__initials { font: 700 2.25rem / 1 var(--font-display); color: var(--gold-text); }
.team-photo--lead {
  flex: none;
  width: 10rem;
  aspect-ratio: 1;
  border-radius: 50%;
}
.team-card {
  height: 100%;
  display: flex;
  flex-direction: column;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--paper);
  overflow: hidden;
  transition: transform var(--t-med) var(--ease), box-shadow var(--t-med) var(--ease);
}
.team-card:hover { transform: translateY(-0.1875rem); box-shadow: var(--shadow-soft); }
.team-card__body { padding: 1.25rem; display: flex; flex-direction: column; gap: 0.625rem; }
.team-card__name { font: 700 1.125rem / 1.25 var(--font-display); color: var(--ink); }
.role-chips { display: flex; flex-wrap: wrap; gap: 0.375rem; }
.role-chip {
  font: 700 0.6875rem / 1 var(--font-ui);
  letter-spacing: 0.04em;
  padding: 0.375rem 0.625rem;
  border-radius: 10rem;
  background: var(--sand);
  color: var(--gold-text);
  white-space: nowrap;
}
```
Delete the `.person`, `.person:hover`, `.person__body`, `.person__name`, `.person__role` rules (`.avatar*`, `.chips`, `.chip*` STAY — used by lead fallback/design-system and Gemeindeleben).

- [x] **Step 2: ueber-uns/index.html** — replace the lead-card `section-cta` block (lines 70–79) with:
```html
      <div class="section-cta" data-ct="team-lead">
        <div class="lead-card">
          <div class="team-photo team-photo--lead"><span class="team-photo__initials">PS</span></div>
          <div class="lead-card__body">
            <div><span class="lead-card__name">Philipp Strauch</span></div>
            <div class="role-chips"><span class="role-chip">Pastor</span><span class="role-chip">Gottesdienst</span><span class="role-chip">Jugendbereich</span></div>
            <div class="lead-card__note"><p>Leitet die Gemeinde seit Oktober 2025. Philipp brennt dafür, das Wort Gottes zu predigen und zu sehen, wie Menschen in ihrer Berufung und ihrem Glauben wachsen.</p></div>
          </div>
        </div>
      </div>
```
Replace the whole `<ul class="team-grid">…</ul>` (lines 80–162) with static fallback in the NEW card markup — one `<li>` per non-featured person from team.json, this exact template:
```html
        <li>
          <div class="team-card">
            <div class="team-photo"><span class="team-photo__initials">XX</span></div>
            <div class="team-card__body">
              <div><span class="team-card__name">NAME</span></div>
              <div class="role-chips"><span class="role-chip">ROLE</span></div>
            </div>
          </div>
        </li>
```
People (initials · name · roles): TS Tymur Sheikh Lobpreis · AB Arne Brockmann Website · LW Lydia Wegner Kindergottesdienst · AM Anja Maas Kindergottesdienst · CW Claus Wittnebel Jugendbereich · UH Ulrike Huhn Hauswirtschaft · GS Günther Seidt Finanzen · SL Saskia Lobert Technik · YU Yurii Uglov Hausmeister. The `<ul>` keeps `class="team-grid"` and gains `data-ct="team"`.

- [x] **Step 3: Verify** — browser `http://localhost:8000/ueber-uns/`: lead card with round photo placeholder + 3 role chips; 9 photo-first cards, each person once; cards equal height per row; 3/2/1 columns across widths. `grep -n "person__" ueber-uns/index.html styles/pages.css` → no matches.

---

### Task 10: Gemeindeleben — declutter + galleries

**Files:**
- Modify: `gemeindeleben/index.html` (Kigo + Elevate sections)
- Modify: `styles/pages.css` (fact strip + gallery)
- Modify: `js/main.js` (gallery init)

**Interfaces:**
- Produces: `.fact-strip`/`.fact`, `.gallery[data-gallery]` with `.gallery__track/__item/__nav` and buttons `[data-gallery-prev]`/`[data-gallery-next]`.

- [x] **Step 1: pages.css** — append:

```css
/* ---- fact strip (Gemeindeleben declutter) ---- */
.fact-strip {
  margin-top: var(--space-xl);
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-md) var(--space-2xl);
  align-items: flex-start;
}
.fact { display: flex; flex-direction: column; gap: 0.375rem; }
.fact__key {
  font: 700 0.75rem / 1.2 var(--font-ui);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
}
.fact__val { font: 700 1.0625rem / 1.4 var(--font-display); color: var(--ink); }

/* ---- gallery (simple snap slider) ---- */
.gallery { margin-top: var(--space-xl); }
.gallery__track {
  display: flex;
  gap: var(--space-md);
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  scrollbar-width: none;
}
.gallery__track::-webkit-scrollbar { display: none; }
.gallery__item {
  flex: 0 0 min(22rem, 78%);
  aspect-ratio: 16 / 10;
  border-radius: var(--radius);
  border: 1px solid var(--line-ink);
  background: var(--sand);
  overflow: hidden;
  scroll-snap-align: start;
}
.gallery__item img { width: 100%; height: 100%; object-fit: cover; }
.gallery__item--tint { background: var(--surface-2); }
.gallery__nav {
  margin-top: var(--space-md);
  display: flex;
  gap: 1rem;
  justify-content: flex-end;
}
@media (max-width: 37.5rem) {
  .fact-strip { gap: var(--space-md) var(--space-xl); }
  .gallery__nav { display: none; } /* touch: swipe */
}
```

- [x] **Step 2: main.js** — append before the closing `})();`:

```js
  /* ---------- Galleries (scroll-snap sliders) ---------- */
  document.querySelectorAll('[data-gallery]').forEach(function (gallery) {
    var track = gallery.querySelector('.gallery__track');
    var prev = gallery.querySelector('[data-gallery-prev]');
    var next = gallery.querySelector('[data-gallery-next]');
    if (!track || !prev || !next) return;
    var behavior = window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth';
    function step() {
      var item = track.firstElementChild;
      if (!item) return 0;
      return item.getBoundingClientRect().width + (parseFloat(getComputedStyle(track).columnGap) || 0);
    }
    prev.addEventListener('click', function () { track.scrollBy({ left: -step(), behavior: behavior }); });
    next.addEventListener('click', function () { track.scrollBy({ left: step(), behavior: behavior }); });
  });
```

- [x] **Step 3: gemeindeleben/index.html — Kindergottesdienst** (replace whole section, lines 63–94):

```html
  <!-- ================= KINDERGOTTESDIENST ================= -->
  <section class="section" id="kindergottesdienst" aria-labelledby="kigo-h">
    <div class="container">
      <div class="section-head">
        <div><span class="eyebrow">Für Kinder</span></div>
        <div class="section-head__title"><h2 class="h2" id="kigo-h">Kindergottesdienst</h2></div>
        <div class="section-head__desc"><p>Während des Gottesdienstes erleben die Kinder ihr eigenes Programm — Geschichten, Musik, Basteln und jede Menge Freude.</p></div>
      </div>
      <div class="fact-strip">
        <div class="fact">
          <div><span class="fact__key">Wann?</span></div>
          <div><span class="fact__val">Sonntags · 10:00 Uhr</span></div>
        </div>
        <div class="fact">
          <div><span class="fact__key">Wo?</span></div>
          <div><span class="fact__val">Parallel zum Gottesdienst, im selben Haus</span></div>
        </div>
        <div class="fact">
          <div><span class="fact__key">Leitung</span></div>
          <div><span class="fact__val">Lydia Wegner &amp; Anja Maas</span></div>
        </div>
      </div>
      <div class="gallery" data-gallery aria-label="Bilder aus dem Kindergottesdienst">
        <div class="gallery__track">
          <div class="gallery__item"><img src="/img/hero-gemeindeleben.jpg" alt="Kinder im Jesus Punkt" loading="lazy"></div>
          <div class="gallery__item"><img src="/img/hero-ueber-uns.jpg" alt="Gemeinschaft im Jesus Punkt" loading="lazy"></div>
          <div class="gallery__item gallery__item--tint" aria-hidden="true"></div>
          <div class="gallery__item" aria-hidden="true"></div>
        </div>
        <div class="gallery__nav">
          <button class="icon-btn" data-gallery-prev aria-label="Vorherige Bilder">
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M9 2 L2 10 L9 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
          <button class="icon-btn" data-gallery-next aria-label="Weitere Bilder">
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M2 2 L9 10 L2 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
        </div>
      </div>
    </div>
  </section>
```

- [x] **Step 4: gemeindeleben/index.html — Elevate** (replace whole section, lines 96–144):

```html
  <!-- ================= ELEVATE ================= -->
  <section class="section section--tint" id="jugend" aria-labelledby="jugend-h">
    <div class="container">
      <div class="section-head">
        <div><span class="eyebrow">Elevate Youth</span></div>
        <div class="section-head__title"><h2 class="h2" id="jugend-h">Die Jugend</h2></div>
        <div class="section-head__desc"><p>ELEVATE ist der Treffpunkt am Freitag für Jugendliche zwischen 13 und 29 Jahren. Coole Aktionen, tiefe Gespräche, gutes Essen — wir sind gemeinsam und mit Gott unterwegs.</p></div>
      </div>
      <div class="fact-strip">
        <div class="fact">
          <div><span class="fact__key">Wann?</span></div>
          <div><span class="fact__val">Jeden Freitag · 18:30 – 21:30 Uhr</span></div>
        </div>
        <div class="fact">
          <div><span class="fact__key">Für wen?</span></div>
          <div><span class="fact__val">13 bis 29 Jahre</span></div>
        </div>
        <div class="fact">
          <div><span class="fact__key">Team</span></div>
          <div><span class="fact__val">Claus, Philipp, Mykhailo &amp; Natan</span></div>
        </div>
        <div class="fact">
          <div><span class="fact__key">Unterwegs</span></div>
          <div><a class="textlink" href="https://www.instagram.com/jesuspunkt" target="_blank" rel="noopener">Auf Instagram folgen <span class="textlink__arrow">→</span></a></div>
        </div>
      </div>
      <div class="gallery" data-gallery aria-label="Bilder aus der Jugend">
        <div class="gallery__track">
          <div class="gallery__item"><img src="/img/hero-events.jpg" alt="Lobpreis bei ELEVATE" loading="lazy"></div>
          <div class="gallery__item"><img src="/img/hero-ueber-uns.jpg" alt="Gemeinschaft bei ELEVATE" loading="lazy"></div>
          <div class="gallery__item gallery__item--tint" aria-hidden="true"></div>
          <div class="gallery__item" aria-hidden="true"></div>
        </div>
        <div class="gallery__nav">
          <button class="icon-btn" data-gallery-prev aria-label="Vorherige Bilder">
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M9 2 L2 10 L9 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
          <button class="icon-btn" data-gallery-next aria-label="Weitere Bilder">
            <svg width="11" height="20" viewBox="0 0 11 20" fill="none" aria-hidden="true"><path d="M2 2 L9 10 L2 18" stroke="currentColor" stroke-width="1.5"/></svg>
          </button>
        </div>
      </div>
    </div>
  </section>
```
Note: ferien info ("Außer in der Ferienzeit") folds into the Events calendar; the note "Termine werden mit ChurchTools synchronisiert" is now implicit sitewide.

- [x] **Step 5: Verify** — `node --check js/main.js` → exit 0. Browser `/gemeindeleben/`: both sections read as head → facts → gallery; arrows slide the gallery; swipe works at 390w (nav hidden); Hauskreise + no Next-Steps; galleries show 2 photos + 2 placeholder tiles each.

---

### Task 11: Spenden + Kontakt cleanup

**Files:**
- Modify: `spenden/index.html`, `kontakt/index.html`
- Modify: `styles/pages.css` (delete `.paypal-btn` rules incl. the `37.5rem` width rule)

- [x] **Step 1: spenden/index.html** — replace BOTH `info-grid info-grid--2` blocks (lines 65–121) with ONE grid, PayPal gone:

```html
      <div class="info-grid info-grid--2">
        <div class="info-card">
          <div><span class="info-card__label">Per Überweisung</span></div>
          <div class="kv">
            <div class="kv__row">
              <div><span class="kv__key">Kontoinhaber</span></div>
              <div><span class="kv__val">VIA Movement e.V.</span></div>
            </div>
            <div class="kv__row">
              <div><span class="kv__key">IBAN</span></div>
              <!-- TODO: IBAN vor Launch eintragen (von der Gemeindeleitung bestätigen lassen) -->
              <div><span class="kv__val">DE·· ···· ···· ···· ···· ··</span></div>
            </div>
            <div class="kv__row">
              <div><span class="kv__key">Verwendungszweck</span></div>
              <div><span class="kv__val">Spende Jesus Punkt</span></div>
            </div>
          </div>
        </div>
        <div class="info-card info-card--accent">
          <div><span class="info-card__label">Fragen zur Spende?</span></div>
          <div class="info-card__text"><p>Schreib uns gern — wir helfen bei Dauerauftrag, Zuwendungsbestätigung und allem Weiteren.</p></div>
          <div class="info-card__foot">
            <a class="textlink" href="mailto:info@jesus-punkt.de?subject=Spende">info@jesus-punkt.de <span class="textlink__arrow">→</span></a>
          </div>
        </div>
        <div class="info-card info-card--tint">
          <div><span class="info-card__label">Anschrift</span></div>
          <div class="kv">
            <div class="kv__row">
              <div><span class="kv__val">Jesus Punkt · VIA Movement e.V.</span></div>
              <div><span class="info-card__text">Kruseshofer Str. 20 · 17036 Neubrandenburg</span></div>
            </div>
            <div class="kv__row">
              <div><span class="kv__key">Mobil</span></div>
              <div><a class="kv__val" href="tel:+4915679133367">+49 156 79133367</a></div>
            </div>
          </div>
        </div>
      </div>
```
Update the page meta description (line 7): drop `oder PayPal` → `Unterstütze die Arbeit von Jesus Punkt Neubrandenburg — per Überweisung.`

- [x] **Step 2: pages.css** — delete the `/* ---- PayPal button ---- */` block (`.paypal-btn`, `:hover`, `svg`) and the `.paypal-btn { width: 100%; … }` line inside `@media (max-width: 37.5rem)`.

- [x] **Step 3: kontakt/index.html** — replace BOTH grids (lines 65–103) with ONE `info-grid info-grid--2` (4 cards, uniform gaps):

```html
      <div class="info-grid info-grid--2">
        <div class="info-card">
          <div><span class="info-card__label">Erreich uns</span></div>
          <div class="kv">
            <div class="kv__row">
              <div><span class="kv__key">Telefon</span></div>
              <div><a class="kv__val" href="tel:+4915679133367">+49 156 79133367</a></div>
            </div>
            <div class="kv__row">
              <div><span class="kv__key">E-Mail</span></div>
              <div><a class="kv__val" href="mailto:info@jesus-punkt.de">info@jesus-punkt.de</a></div>
            </div>
          </div>
          <div class="info-card__text"><p>Wir antworten so schnell wie möglich — und rufen gern zurück.</p></div>
        </div>
        <div class="info-card info-card--accent">
          <div><span class="info-card__label">Gottesdienst</span></div>
          <div><span class="info-card__value">Sonntags · 10:00 Uhr</span></div>
          <div class="info-card__text"><p>Vor Ort und im Livestream auf YouTube.</p></div>
          <div class="info-card__foot">
            <a class="textlink" href="https://www.youtube.com/@jesuspunkt/streams" target="_blank" rel="noopener">Zum Livestream <span class="textlink__arrow">→</span></a>
          </div>
        </div>
        <div class="info-card info-card--tint">
          <div><span class="info-card__label">Adresse</span></div>
          <div><span class="info-card__value">Kruseshofer Str. 20<br>17036 Neubrandenburg</span></div>
          <div class="info-card__text"><p>Parkplätze direkt am Haus.</p></div>
          <div class="info-card__foot">
            <a class="textlink" href="https://maps.google.com/?q=Kruseshofer+Str.+20,+17036+Neubrandenburg" target="_blank" rel="noopener">Route planen <span class="textlink__arrow">→</span></a>
          </div>
        </div>
        <div class="info-card">
          <div><span class="info-card__label">Social Media</span></div>
          <div class="info-card__text"><p>Bleib auf dem Laufenden — Predigten, Events und Einblicke ins Gemeindeleben.</p></div>
          <div class="kv">
            <div><a class="textlink" href="https://www.youtube.com/@jesuspunkt" target="_blank" rel="noopener">YouTube <span class="textlink__arrow">→</span></a></div>
            <div><a class="textlink" href="https://www.instagram.com/jesuspunkt" target="_blank" rel="noopener">Instagram <span class="textlink__arrow">→</span></a></div>
            <div><a class="textlink" href="https://www.facebook.com/jesuspunkt" target="_blank" rel="noopener">Facebook <span class="textlink__arrow">→</span></a></div>
          </div>
        </div>
      </div>
```

- [x] **Step 4: Verify** — `grep -rn "paypal" spenden/index.html styles/pages.css` (case-insensitive) → no matches. Browser: /spenden/ 3 cards no PayPal; /kontakt/ 2×2 grid, equal gaps, stacks to 1 column ≤37.5rem.

---

### Task 12: Docs + design-system + CLAUDE.md

**Files:**
- Modify: `design-system.html`, `CLAUDE.md`, `docs/churchtools-integration.md`

- [x] **Step 1: design-system.html** — read the file, then: update the header demo markup to match Task 5 (glider note, is-active dot, no strip); remove any Next-Steps/`step`/`steps-grid` demo; remove `person` demo if present, add a `team-card` + `role-chip` example (copy Task 9 markup); add short demo blocks for `.fact-strip`, `.gallery` (2 tiles), `.calendar__bar`, and a note that the Werte wheel is scroll-driven (link to homepage) — static wheel markup with one `is-active` item is enough. Keep it a living styleguide: every component that now exists in production appears here once.

- [x] **Step 2: CLAUDE.md** — apply these edits:
  - Header Rules section: replace the first bullet with: `Header is **fixed and always transparent** — after 8px of scroll (.is-scrolled, toggled in js/main.js) the floating elements (nav pill, logo chip, burger) get stronger white surfaces + hairline + shadow. No full-width strip.`
  - Replace the current-page bullet with: `Current page indicator = ink text + small gold dot after the label (no background). Hover = single gold glider pill that slides between links (JS-positioned).`
  - Interaction Rules: add `Werte section is a pinned scroll wheel (sticky 100dvh stage inside a 350vh section); reduced-motion gets a static list.`
  - Repo Layout: `js/main.js` line becomes `js/main.js  header state, nav glider, fullscreen nav, carousels, Werte wheel, galleries · js/data.js  renders data-ct slots from data/mock/*.json (swap URLs for live ChurchTools)`.
  - Remove the sentence naming the Werte bubble field as explicit client request (superseded by the wheel; the interaction layer mention stays).
- [x] **Step 3: churchtools-integration.md** — append this section verbatim:

```markdown
## Content-Pflege (Kurationsstrategie)

**Grundsatz: ChurchTools ist die einzige Pflegeoberfläche für alles, was sich wöchentlich ändert.**
Termine, Flyer, Predigten, Hauskreise und Personen/Rollen werden dort gepflegt, wo das Team
ohnehin täglich arbeitet — niemand muss ein neues System lernen. Die Website liest diese Daten
nur (Endpoint-Map oben); `js/data.js` rendert sie in die `data-ct`-Slots.

| Inhalt | Pflegeort | Wer |
|---|---|---|
| Termine / Kalender | ChurchTools Kalender | Sekretariat / Bereichsleiter |
| Flyer | Anhang am Sonntags-Termin | Sekretariat |
| Predigten | YouTube (Proxy zieht Metadaten) | Technik-Team |
| Hauskreise | ChurchTools Gruppen | Hauskreis-Koordination |
| Team-Seite (Personen + Rollen) | ChurchTools Personen/Gruppenrollen | Gemeindeleitung |
| Statische Texte (Hero, Werte, Glaube …) | Repo (Entwickler) | Website-Team |

**Bewusst NICHT geplant:** ein vollwertiges Headless-CMS (Strapi, Sanity, Contentful).
Gründe: zweites System neben ChurchTools (doppelte Pflege, Drift), Hosting-/Lizenzkosten,
Einarbeitung. Statische Texte ändern sich selten genug, dass der Weg über das Website-Team
schneller ist als jede CMS-Schulung.

**Optionaler späterer Ausbau:** ein Git-basiertes Mini-CMS (Sveltia CMS oder Pages CMS —
kostenlos, keine Server, Browser-Editor auf JSON/Markdown-Dateien im Repo) ausschließlich
für die statischen Texte. Lernaufwand: Minuten; Änderungen laufen als Commits durch dieselbe
Pipeline wie Code.
```

- [x] **Step 4: Verify** — `grep -n "nav-panel__num\|next-steps\|steps-grid\|page-head\|\.person" design-system.html` → no matches; CLAUDE.md renders sensibly (read it once fully).

---

### Task 13: Responsive verification pass

**Files:** none created (screenshots go to `docs/superpowers/plans/shots/`, throwaway).

- [x] **Step 1: screenshot matrix** (server running):
```bash
mkdir -p docs/superpowers/plans/shots && node tools/screenshot.js '[
 {"url":"http://localhost:8000/","w":1440,"h":6800,"out":"docs/superpowers/plans/shots/home-1440.png"},
 {"url":"http://localhost:8000/","w":1024,"h":6800,"out":"docs/superpowers/plans/shots/home-1024.png"},
 {"url":"http://localhost:8000/","w":768,"h":7600,"out":"docs/superpowers/plans/shots/home-768.png"},
 {"url":"http://localhost:8000/","w":390,"h":8600,"out":"docs/superpowers/plans/shots/home-390.png"},
 {"url":"http://localhost:8000/events/","w":1440,"h":3200,"out":"docs/superpowers/plans/shots/events-1440.png"},
 {"url":"http://localhost:8000/events/","w":390,"h":4200,"out":"docs/superpowers/plans/shots/events-390.png"},
 {"url":"http://localhost:8000/ueber-uns/","w":1440,"h":4200,"out":"docs/superpowers/plans/shots/team-1440.png"},
 {"url":"http://localhost:8000/ueber-uns/","w":390,"h":6200,"out":"docs/superpowers/plans/shots/team-390.png"},
 {"url":"http://localhost:8000/gemeindeleben/","w":1440,"h":3600,"out":"docs/superpowers/plans/shots/gl-1440.png"},
 {"url":"http://localhost:8000/gemeindeleben/","w":390,"h":5200,"out":"docs/superpowers/plans/shots/gl-390.png"},
 {"url":"http://localhost:8000/kontakt/","w":390,"h":3200,"out":"docs/superpowers/plans/shots/kontakt-390.png"},
 {"url":"http://localhost:8000/spenden/","w":768,"h":2800,"out":"docs/superpowers/plans/shots/spenden-768.png"}
]'
```
Read every screenshot. Checklist per shot: no horizontal overflow (content cut at right edge = fail); heroes readable; 3 event rows on home (Gottesdienst first, gold); equal flyer/list columns ≥64rem, stacked below 48rem; calendar bar intact at 390; team grid 3/2/1; galleries swipeable width at 390; footer roomy; no orphaned dead space where Next-Steps was.
- [x] **Step 2: interaction sanity in a real browser** (claude-in-chrome or manual): glider slide between nav links; wheel pin + 7 stops + unpin; calendar month stepping; both carousels; both galleries; burger menu (no digits, is-active states); reduced-motion pass (System Settings → Accessibility) for wheel + glider.
- [x] **Step 3: content-resilience spot check** — temporarily edit `data/mock/events.json` to leave only 2 future events → home list shows 2 rows without layout break, calendar shows empty months correctly; restore the file afterwards (verify with `python3 -c "import json;json.load(open('data/mock/events.json'));print('OK')"`).
- [x] **Step 4: fix anything found, re-shoot the failing breakpoint, and record a one-line result per page/breakpoint in the task notes.**

---

## Self-review notes

- Spec §1–§12 all covered: data layer (T1–T2), homepage (T3), rhythm (T3), wheel (T6), header (T5), heroes (T7), events page (T8), team (T9), curation doc (T12), Gemeindeleben (T10), small fixes (T3/T4/T11), responsive (T13).
- Cross-task names checked: `JPCarousel.refresh` (T2↔T8), `cal-prev/next/label` + `data-calendar` (T2↔T8), `team-lead`/`team` slots + `.team-card`/`.role-chip` classes (T2↔T9), `.events__list` move (T8), bubble variants kept for wheel (T6).
- Known sequencing: T2 ships `renderSermons` before T8 replaces the events-page grid — safe because homepage track exists and the guard on `window.JPCarousel` tolerates the pre-T8 main.js.
